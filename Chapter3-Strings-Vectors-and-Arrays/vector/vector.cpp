#include <vector>
#include <string>

using std::vector;
using std::string;

int main(){
  
  vector<int> ivec;
  
  vector<vector<string>> file; // in c++98 vector<vector<string> > we must supply an extra space between the close angle bracket
  
  return 0;
}
